{
  "name": "crank",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "api:test": "npm run test -w crank-api",
    "api:test:watch": "npm run test:watch -w crank-api",
    "api:test:integration": "npm run test:integration -w crank-api",
    "api:test:coverage": "npm run test:coverage -w crank-api",
    "api:dev": "npm run dev -w crank-api",
    "api:dev:worker": "npm run dev:worker -w crank-api",
    "api:build": "npm run build -w crank-api",
    "api:start": "npm run start -w crank-api",
    "api:start:worker": "npm run start:worker -w crank-api"
  }
}
